package cron

import (
	// "btc-network-monitor/internal/adapter/api/resource"
	// "btc-network-monitor/internal/adapter/api/rpc"
	// mysql_repo "btc-network-monitor/internal/adapter/repositories/mysql"
	// "btc-network-monitor/internal/core/services"
	// "fmt"

	// "github.com/btcsuite/btcd/chaincfg/chainhash"
)


func txNotify (){
	
	// client := rpc.Config

	// txSubs := services.TxSubscribeService{}
	// subs, err := txSubs.GetAll(map[string]interface{}{})
	// subSlices := subs.([]string)
	// for _, sub := range subSlices {
	// 	txId, err := chainhash.NewHashFromStr(sub.tx_id)
	// 	if err != nil {
	// 		fmt.Println("Error sending transaction:", err)
	// 		return
	// 	}
	// 	getTx, err := client.GetTransaction(txId)
	// 	if err != nil {
	// 		fmt.Println("Error sending json transaction:", err)
	// 		return
	// 	}

	// 	if getTx.Confirmations > 6 {
	// 		//send email
	// 	}
	// }
	
}